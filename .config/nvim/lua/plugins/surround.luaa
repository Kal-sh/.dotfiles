return {
  "kylechui/nvim-surround",
  version = "*", -- Use for stability; omit to use `main` branch
  event = "VeryLazy",
  config = function()
    require("nvim-surround").setup({
      -- Empty setup block is fine for default behavior
    })
  end,
}
